# DATABASE_URL para Render / local
# - En Render: usar la Internal Database URL
# - En local: usar la External Database URL
# Ejemplos de formato (NO pegar credenciales reales aquí):
DATABASE_URL=postgresql://<USER>:<PASSWORD>@<HOST>/<DB_NAME>

# Ejemplo Internal (Render):
# postgresql://<USER>:<PASSWORD>@<INTERNAL_HOST>/<DB_NAME>
# Ejemplo External (Render):
# postgresql://<USER>:<PASSWORD>@<EXTERNAL_HOST>.region-postgres.render.com/<DB_NAME>

# Control de SSL (opcional)
# - Deja vacío para usar el valor por defecto del código
# - "true" / "1" / "on": fuerza SSL laxo (por defecto)
# - "strict": SSL con verificación estricta de certificado
# - "false" / "0" / "off": deshabilita SSL (no recomendado)
DB_SSL=true
